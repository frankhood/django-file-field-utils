============
Installation
============

At the command line::

    $ easy_install django-file-field-utils

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-file-field-utils
    $ pip install django-file-field-utils
